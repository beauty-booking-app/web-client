# 04 · Pre-Footer Banner — Tareas

_Checklist accionable derivada del `plan.md`. Marca `[x]` al completarlas._

- [x] Crear `src/components/PreFooterBanner.jsx`.
- [x] Renderizar sección con fondo `--primary`, padding consistente.
- [x] Implementar layout horizontal (`lg:flex-row`) con `items-center justify-between`.
- [x] Renderizar volanta + título a la izquierda en color blanco.
- [x] Renderizar botón CTA a la derecha con fondo blanco, texto `--primary`, targets ≥ 44px.
- [x] Botón CTA apunta a `/reserva` con `aria-label` descriptivo.
- [x] Importar `<PreFooterBanner />` en `src/App.jsx` debajo de `<About />`.
- [x] Validar `pnpm build` sin errores.
- [x] Validar `pnpm lint` sin errores.
- [x] Verificar que el botón CTA tiene targets ≥ 44px.
- [x] Validar contra los criterios de aceptación de `spec.md`.
- [x] Actualizar `docs/CAMBIOS.md`.
